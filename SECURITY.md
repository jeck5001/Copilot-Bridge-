# 安全策略

## 支持范围

安全修复面向当前 `main` 分支和最新正式版本。过期版本应先在备份后升级，再验证问题是否仍然存在。

## 报告漏洞

优先使用 GitHub Security Advisory 的私密报告功能。报告应包含受影响版本、攻击前提、最小复现、实际影响和建议缓解方式。

不得在报告中提交：

- access token、refresh token、API Key 或管理员密码；
- `accounts.json`、`sessions.json`、`api-keys.json` 或完整 `data/`；
- OAuth 完整回调 URL、HAR、Cookie、证书私钥或云凭据；
- 真实邮箱、租户 ID、账号 ID、域名、IP、代理地址或生产日志。

请使用不可用的占位符，并在必要时仅提供最小、脱敏片段。

## 安全部署基线

1. 服务默认绑定 `127.0.0.1`；不要直接公开 4141。
2. 首次登录使用公开引导密码 `admin888`，必须立即修改。服务会在修改前锁定其他管理操作，重启不会解除强制修改状态。
3. 公网访问必须通过 HTTPS 反向代理，并设置 `M365_COOKIE_SECURE=true`。
4. 只开放必需端口，启用主机防火墙和系统安全更新。
5. 使用非 root 用户、只读根文件系统、最小 capabilities 和独立数据卷。
6. API Key 应按客户端分开创建，设置合理有效期，泄露后立即撤销。
7. 限制管理页面的来源网络；高风险环境可在反向代理增加 VPN、mTLS 或额外身份认证。
8. 不要把 `.env`、数据卷、日志和备份提交到版本控制。
9. 调试等级只在短时间排障时开启；协议追踪可能包含敏感上下文。
10. 备份必须加密，并定期验证恢复流程。

## 凭据存储说明

- 管理员密码以 bcrypt 哈希保存。
- 网关 API Key 完整值只在创建时返回，落盘为 SHA-256 哈希及元数据。
- Microsoft OAuth access/refresh token 为实现账号接入所必需，会保存在账号缓存中；该文件必须视为高敏感秘密。
- 文件存储通过临时文件、同步和原子重命名降低中途崩溃导致的截断风险；这不等同于静态加密。

如需静态加密，请使用加密磁盘、加密卷或操作系统密钥管理方案。不要自行把主密钥与密文放在同一个公开目录。

## 反向代理信任边界

应用只有在直接对端为回环地址时才信任 `X-Forwarded-For`。因此推荐让 Nginx/Caddy 与应用位于同一主机并让应用仅监听回环地址。若跨主机代理，请同时配置网络访问控制，避免攻击者绕过代理直接访问应用。

## 默认不公开的诊断数据

上游原始事件默认不加入兼容响应。不要启用 `M365_INCLUDE_UPSTREAM_EVENTS`、`M365_TRACE_PROTOCOL` 或 `M365_ROUTER_DEBUG` 作为长期生产配置；这些选项只用于受控排障，结束后应关闭并清理相关日志。

## 发布净化

正式公开包应通过 [PUBLIC_RELEASE_AUDIT.md](PUBLIC_RELEASE_AUDIT.md) 中的检查，并确认 Git 工作树、生成压缩包以及将要推送的完整 Git 历史都不含秘密。
