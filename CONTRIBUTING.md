# 贡献指南

感谢参与 M365 Gateway。

## 开发流程

1. 从最新 `main` 创建独立分支。
2. 一个提交或 PR 聚焦一个可验证问题。
3. 修复行为问题时先添加失败测试，再修改实现。
4. 执行 `gofmt`、`go test ./...`、`go test -race ./...`、`go vet ./...`。
5. 说明兼容性影响、失败分类、回滚方法和实际验证范围。

## 代码要求

- 不吞掉错误，不把不同生产故障统一成一个模糊错误。
- 流式接口必须以成功终止或明确失败事件结束。
- 工具调用与工具结果必须保持 ID、顺序和会话边界。
- 账号选择必须确定、可观察，并避免无意义地消耗全部账号。
- 修改持久化格式时必须覆盖崩溃恢复、并发和旧格式迁移。
- 不以提高超时或无限重试代替根因修复。
- 新增配置应有默认值、边界校验、文档和测试。

## 禁止提交的内容

不得提交任何真实 token、Cookie、API Key、账号缓存、邮箱、租户 ID、代理凭据、HAR、证书私钥、生产域名/IP、服务器拓扑、日志或备份。测试必须使用明显不可用的示例值。

如需报告安全漏洞，请遵循 [SECURITY.md](SECURITY.md)，不要创建包含秘密的公开 Issue。

## 许可证

提交贡献即表示你有权提交相应代码，并同意其按项目的 AGPL-3.0-only 许可证发布，同时保留适用的第三方版权声明。
