# 公开发行净化审计

版本：1.0.0  
目标：创建一个与任何生产服务器、个人账号和既有运行状态无关的空白发行版。

## 已纳入

- `cmd/` 与 `internal/` 中可编译的 Go 源码和测试
- 独立品牌管理页面 `web/index.html`、`web/admin.js`
- 通用 Dockerfile、Docker Compose 和本地运行脚本
- 完整安装、配置、安全、免责声明、许可证和贡献文档
- GitHub Actions 的格式、测试、竞态、静态检查和构建流程

## 明确排除

- 生产 `ops/`、服务器编号、固定 IP/域名、证书和 Nginx 站点配置
- 账号缓存、邮箱、OID/TID、OAuth access/refresh token
- API Key 原文或生产 API Key 哈希库
- 代理地址、代理认证、隧道端口和账号到代理的映射
- 会话、统计、账号路由状态、调试日志和压力测试输出
- 服务器备份、容器镜像、数据卷、恢复包和生产报告
- 本机用户名、桌面路径、快捷方式名和真实项目路径
- `.git` 历史中的旧公开快照；1.0.0 使用新的根提交初始化

## 初始运行状态

- 账号数：0
- API Key 数：0
- 会话数：0
- 代理数：0
- 日志条数：0
- 持久化数据：不存在，由程序首次运行时按需创建
- 引导管理员密码：`admin888`，首次登录后强制修改

## 发布前门禁

每次发布至少执行：

```bash
gofmt -w cmd internal
go test ./...
go test -race ./...
go vet ./...
go build -trimpath ./cmd/server
```

此外必须：

1. 扫描工作树中的 token、密钥、邮箱、私网/公网 IP、域名、证书和个人路径；
2. 创建源码归档后解压到临时目录再次扫描；
3. 扫描准备推送的完整 Git 历史，而不仅是当前文件；
4. 确认 `git status` 干净、远端提交与本地提交一致；
5. 记录源码归档 SHA-256；
6. 不使用真实账号执行公开 CI。

本文件描述的是发行流程，不代表软件不存在未知漏洞。部署者仍需遵循 [SECURITY.md](SECURITY.md)。
